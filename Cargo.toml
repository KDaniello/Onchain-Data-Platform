[workspace]
members = [
    "crates/common",
    "services/ingest",
    "services/decode",
    "services/api",
    "services/finalizer"
]
resolver = "2"

# Общие настройки профилей для оптимизации
[profile.dev]
opt-level = 0
debug = 1

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
